// Simple API endpoint for testing CORS
let {response, method} = import @basil/http

response.headers["Content-Type"] = "application/json"
response.headers["X-Total-Count"] = "42"
"{\"message\": \"CORS is working!\", \"method\": \"" + method + "\"}"
