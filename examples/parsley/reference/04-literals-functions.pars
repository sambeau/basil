// === SECTION 1.8: Functions ===

// --- Anonymous functions ---
let double = fn(x) { x * 2 }
double(5)

let add = fn(a, b) { a + b }
add(3, 4)

let constant = fn() { 42 }
constant()

// --- Implicit return (last expression) ---
let complex = fn(x) {
    let y = x * 2
    y + 1
}
complex(10)
