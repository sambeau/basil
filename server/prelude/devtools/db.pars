<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8"/>
	<meta name="viewport" content="width=device-width, initial-scale=1"/>
	<title>Database Browser</title>
	<style>
		* { box-sizing: border-box; margin: 0; padding: 0; }
		body {
			font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
			background: #1a1a2e;
			color: #eee;
			padding: 2rem;
			min-height: 100vh;
		}
		.container {
			max-width: 1200px;
			margin: 0 auto;
		}
		.header {
			display: flex;
			justify-content: space-between;
			align-items: center;
			margin-bottom: 1.5rem;
		}
		h1 {
			font-size: 1.5rem;
			color: #4ecdc4;
		}
		.btn {
			background: #16213e;
			color: #eee;
			padding: 0.5rem 1rem;
			border-radius: 4px;
			text-decoration: none;
			font-size: 0.875rem;
			border: 1px solid #4ecdc4;
			transition: background 0.2s;
		}
		.btn:hover {
			background: #4ecdc4;
			color: #1a1a2e;
		}
		.stats {
			background: #16213e;
			border-radius: 8px;
			padding: 1rem 1.25rem;
			margin-bottom: 1rem;
			border-left: 4px solid #4ecdc4;
			color: #bbb;
			font-size: 0.875rem;
		}
		.empty-state {
			background: #16213e;
			border-radius: 8px;
			padding: 3rem;
			text-align: center;
			color: #7f8c8d;
		}
		.table-list {
			display: grid;
			gap: 1rem;
		}
		.table-card {
			background: #16213e;
			border-radius: 8px;
			padding: 1.25rem;
			border-left: 4px solid #4ecdc4;
			text-decoration: none;
			color: inherit;
			transition: transform 0.2s, border-color 0.2s;
		}
		.table-card:hover {
			transform: translateX(4px);
			border-color: #ff6b6b;
		}
		.table-name {
			font-size: 1.125rem;
			font-weight: 600;
			color: #4ecdc4;
			margin-bottom: 0.5rem;
			font-family: 'Monaco', 'Courier New', monospace;
		}
		.table-card:hover .table-name {
			color: #ff6b6b;
		}
		.table-info {
			color: #7f8c8d;
			font-size: 0.875rem;
		}
		.error-state {
			background: #16213e;
			border-radius: 8px;
			padding: 2rem;
			border-left: 4px solid #ff6b6b;
		}
		.error-title {
			color: #ff6b6b;
			font-size: 1.125rem;
			margin-bottom: 0.5rem;
		}
		.error-message {
			color: #bbb;
			font-size: 0.875rem;
		}
		.db-actions {
			display: flex;
			gap: 1rem;
			margin-bottom: 1rem;
		}
		.btn-download {
			background: #4ecdc4;
			color: #1a1a2e;
			padding: 0.75rem 1.5rem;
			border-radius: 4px;
			text-decoration: none;
			font-size: 0.875rem;
			font-weight: 600;
			border: none;
			cursor: pointer;
			transition: background 0.2s;
		}
		.btn-download:hover {
			background: #3db8af;
		}
		.upload-form {
			display: flex;
			gap: 0.5rem;
			align-items: center;
		}
		.file-input {
			display: none;
		}
		.file-label {
			background: #16213e;
			color: #eee;
			padding: 0.75rem 1.5rem;
			border-radius: 4px;
			font-size: 0.875rem;
			border: 1px solid #4ecdc4;
			cursor: pointer;
			transition: background 0.2s;
		}
		.file-label:hover {
			background: #4ecdc4;
			color: #1a1a2e;
		}
		.btn-upload {
			background: #ff6b6b;
			color: #fff;
			padding: 0.75rem 1.5rem;
			border-radius: 4px;
			font-size: 0.875rem;
			font-weight: 600;
			border: none;
			cursor: pointer;
			transition: background 0.2s;
			display: none;
		}
		.btn-upload:hover {
			background: #ff5252;
		}
		.btn-upload.visible {
			display: block;
		}
		.file-name {
			font-size: 0.875rem;
			color: #bbb;
		}
		.upload-message {
			margin-top: 0.5rem;
			padding: 0.5rem;
			border-radius: 4px;
			font-size: 0.875rem;
			display: none;
		}
		.upload-message.success {
			background: #2ecc71;
			color: #fff;
			display: block;
		}
		.upload-message.error {
			background: #ff6b6b;
			color: #fff;
			display: block;
		}
	</style>
</head>
<body>
	<div class="container">
		<div class="header">
			<h1>🗄️ Database Browser</h1>
			<a href="/__/" class="btn">← Back</a>
		</div>

		<div class="db-actions">
			<a href="/__/db/download" class="btn-download">📥 Download Database</a>
			<form class="upload-form" id="uploadForm" enctype="multipart/form-data">
				<input type="file" id="fileInput" name="database" accept=".db,.sqlite,.sqlite3" class="file-input"/>
				<label for="fileInput" class="file-label">📤 Choose Database File</label>
				<span class="file-name" id="fileName"></span>
				<button type="submit" class="btn-upload" id="uploadBtn">Upload</button>
			</form>
		</div>
		<div id="uploadMessage" class="upload-message"></div>

		{if (devtools.error) {
			<div class="error-state">
				<div class="error-title">Database Error</div>
				<div class="error-message">{devtools.error}</div>
			</div>
		}}
		{if (!devtools.error && devtools.table_count == 0) {
			<div class="empty-state">
				No tables found in database.
			</div>
		}}
		{if (!devtools.error && devtools.table_count > 0) {
			<div class="stats">
				{devtools.table_count} {if (devtools.table_count == 1) { "table" } else { "tables" }} in database
			</div>

			<div class="table-list">
				for (table in devtools.tables) {
					<a href="/__/db/view/{table.name}" class="table-card">
						<div class="table-name">{table.name}</div>
						<div class="table-info">{table.row_count} {if (table.row_count == 1) { "row" } else { "rows" }}</div>
					</a>
				}
			</div>
		}}
	</div>
	<script>
		// Handle file selection
		const fileInput = document.getElementById('fileInput');
		const fileName = document.getElementById('fileName');
		const uploadBtn = document.getElementById('uploadBtn');
		const uploadMessage = document.getElementById('uploadMessage');

		fileInput.addEventListener('change', function(e) {
			if (e.target.files.length > 0) {
				fileName.textContent = e.target.files[0].name;
				uploadBtn.classList.add('visible');
			} else {
				fileName.textContent = '';
				uploadBtn.classList.remove('visible');
			}
		});

		// Handle form submission
		document.getElementById('uploadForm').addEventListener('submit', async function(e) {
			e.preventDefault();
			
			const file = fileInput.files[0];
			if (!file) return;

			uploadMessage.className = 'upload-message';
			uploadMessage.textContent = 'Uploading...';
			uploadMessage.style.display = 'block';

			const formData = new FormData();
			formData.append('database', file);

			try {
				const response = await fetch('/__/db/upload', {
					method: 'POST',
					body: formData
				});

				const result = await response.json();

				if (response.ok) {
					uploadMessage.className = 'upload-message success';
					uploadMessage.textContent = result.message || 'Database uploaded successfully';
					// Reload page after 1 second
					setTimeout(() => window.location.reload(), 1000);
				} else {
					uploadMessage.className = 'upload-message error';
					uploadMessage.textContent = result.message || 'Upload failed';
				}
			} catch (error) {
				uploadMessage.className = 'upload-message error';
				uploadMessage.textContent = 'Upload failed: ' + error.message;
			}
		});
	</script>
</body>
</html>
