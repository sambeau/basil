// Debug the destructuring issue

let TextField = fn(props) {
    let {name, label, type, value, hint, error, required, id, class, ...inputAttrs} = props
    
    // Debug: print what we extracted
    print("name:", name)
    print("label:", label)
    print("type:", type)
    print("value:", value)
    print("required:", required)
    print("inputAttrs:", inputAttrs)
    print("---")
    
    <input 
        name={name}
        type={type ?? "text"}
        value={value ?? ""}
        ...inputAttrs
    />
}

<TextField name="search" label="Search" type="search" value="" placeholder="search" required={false}/>
