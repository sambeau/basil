---
id: man-(pars|bas)-(builtin-name) e.g. (man-pars-array)
title: “Manual Page Title" e.g. “Arrays”
system: parsley | basil 
type: builtin | stdlib | feature | tutorial e.g.(‘builtin’)
Name: name of feature in code (e.g. array)
created: YYYY-MM-DD
version: xx.xx.xx (version of basil when this was generated - without git id)
author: “@sam”
keywords: a, few, keywords, like, array
---

## Manual Page Title (for instance, “Arrays”)

One paragraph describing what this object or builtin is.

```parsley
A small, tested, peice of code showing a typical usage scenario. 
```

## Operators

### Operator (e.g. ++)

One paragraph describing what this operator is, how it manipulates the builtin, and—if relevant—what it’s *useful* for. If there are overloads on the operator provide examples of them too.

```parsley
A small, tested, peice of code showing a typical usage scenario. 
```

Results (if relevant):

```parsley
The result from the peice of code, if there are any. 
```

:

## Attributes

(All attributes listed alphabetically)

### attribute (e.g. columns)

#### Usage 

One paragraph describing what this operator is, and—if relevant—what it’s *useful* for.

```parsley
A small, tested, peice of code showing a typical usage scenario. 
```

Results (if relevant):

```parsley
A small, tested, peice of code showing a typical usage scenario. 
```

:

## Methods

(All methods listed alphabetically)

### Method() (e.g. sortBy())

#### Usage with arguments (e.g. sortBy(fn))

One paragraph describing what this operator is, and—if relevant—what it’s *useful* for.

```parsley
A small, tested, peice of code showing a typical usage scenario. 
```

Results (if relevant):

```parsley
A small, tested, peice of code showing a typical usage scenario. 
```

:

### @stdlib

Any methods in the standard lib that work on this builtin

(All methods listed alphabetically as links to the man page for that stdlib e.g. eg @std/math will be 

- [@std/math.mean](/manual/std/math/mean)()
- :


