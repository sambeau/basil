---
id: man-(pars|bas)-(builtin-name) e.g. (man-pars-std-math)
title: “Manual Page Title" e.g. "Std/Math"
system: parsley | basil (e.g. parsley)
type: builtin | stdlib | feature | tutorial e.g.(stdlib)
Name: name of feature in code (e.g. @std/math)
created: YYYY-MM-DD
version: xx.xx.xx (version of basil when this was generated - without git id)
author: “@sam”
keywords: a, few, keywords, like, std, math
---

## Manual Page Title (for instance, "Std/Math Library")

One paragraph overview describing what this library is and, if it built around a pseudo-type, what that type is and is for. Breifly touch upon typical usage.

```parsley
A small, tested, peice of code showing a typical usage scenario. 
```

## Attributes

(All attributes listed alphabetically)

### attribute (e.g. columns)

#### Usage 

One paragraph describing what this operator is.

```parsley
A small, tested, peice of code showing a typical usage scenario. 
```

Results (if relevant):

```parsley
A small, tested, peice of code showing a typical usage scenario. 
```

:

## Methods

(All methods listed alphabetically)

### Method() (e.g. sortBy())

#### Usage with arguments (e.g. sortBy(fn))

One paragraph describing what this operator is, and—if relevant—what it’s *useful* for.

```parsley
A small, tested, peice of code showing a typical usage scenario. 
```

Results (if relevant):

```parsley
A small, tested, peice of code showing a typical usage scenario. 
```

: